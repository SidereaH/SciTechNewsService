spring:
  profiles:
    active: dev
  application:
    name: SciTechNewsService
  datasource:
    url: jdbc:postgresql://localhost:5432/scitech_newsservice
    username: "postgres"
    password: "postgres"
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: create-drop
server:
  port: 8083



